{
  "name": "rochade",
  "private": true,
  "type": "module",
  "scripts": {
    "api:types": "pnpm --filter @rochade/api-client run generate",
    "dev:hall": "pnpm --filter @rochade/hall run dev",
    "dev:admin": "pnpm --filter @rochade/admin run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.27.0"
}
