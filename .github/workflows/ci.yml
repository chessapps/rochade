name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: rochade
          POSTGRES_PASSWORD: rochade
          POSTGRES_DB: rochade
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U rochade"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      ROCHADE_TEST_DATABASE_URL: postgresql+psycopg://rochade:rochade@127.0.0.1:5432/rochade
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv venv
      - run: uv pip install -e ".[dev]"
      - name: Lint
        run: |
          uv run ruff check src tests spikes scripts
          uv run ruff format --check src tests spikes scripts
      - name: Types
        run: uv run mypy
      - name: Tests
        run: uv run pytest -q
      - name: Migrations match the models
        env:
          ROCHADE_DATABASE_URL: postgresql+psycopg://rochade:rochade@127.0.0.1:5432/rochade
        run: |
          uv run alembic upgrade head
          uv run alembic check

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm -r run typecheck
      - run: pnpm -r run test
      - run: pnpm -r run build

  contract:
    # The generated client is checked in, so a backend change that alters the
    # API must show up as a diff here rather than as a runtime surprise.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: uv venv && uv pip install -e "."
      - run: uv run python scripts/dump_openapi.py
      - run: pnpm install --frozen-lockfile
      - run: pnpm run api:types
      - name: The checked-in client matches the API
        run: git diff --exit-code packages/api-client/src/schema.d.ts
