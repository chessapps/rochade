"""What anyone may read: a published tournament, by slug, with nothing else.

Every query here is `Access.PUBLIC`, answers by slug rather than id, and
knows nothing about phones, codes, claims or members. An unpublished
tournament is not found, exactly like one that never existed.
"""
